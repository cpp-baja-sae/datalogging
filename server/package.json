{
  "name": "kitkat-server",
  "version": "1.0.0",
  "description": "Server which provides REST endpoints used by the frontend. Also serves the frontend.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Maros",
  "license": "ISC",
  "dependencies": {
    "abstract-socket": "^2.1.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "ws": "^7.2.1"
  }
}
